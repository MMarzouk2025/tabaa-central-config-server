# server
server:
  port: 8888

# spring cloud config server
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MMarzouk2025/tabaa-central-config-server
          search-paths: config
